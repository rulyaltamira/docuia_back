# cleanup.yml - Archivo temporal para eliminar el servicio antiguo
service: docpilot-newsystem

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.9
  region: eu-west-1
  stage: ${opt:stage, 'dev'}

# Este archivo está vacío a propósito - solo se usa para eliminar el servicio existente 